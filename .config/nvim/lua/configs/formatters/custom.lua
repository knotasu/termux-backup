-- Custom formatters configuration
-- Add your custom formatters here

return {
  formatters = {
    -- Example: add a custom formatter
    -- my_custom_formatter = {
    --   command = "my-formatter",
    --   args = { "--option", "$FILENAME" },
    -- },
  },

  formatters_by_ft = {
    -- Example: add formatter for a specific filetype
    -- python = { "black" },
  },
}
